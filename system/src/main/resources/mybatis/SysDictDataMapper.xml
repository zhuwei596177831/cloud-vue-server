<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.system.mapper.SysDictDataMapper">

    <sql id="selectDictDataVo">
        select dictCode,
               dictSort,
               dictLabel,
               dictValue,
               dictType,
               cssClass,
               listClass,
               isDefault,
               status,
               createBy,
               createTime,
               remark
        from `system`.sys_dict_data
    </sql>

    <select id="selectDictDataList" resultType="com.example.system.entity.SysDictData">
        <include refid="selectDictDataVo"/>
        <where>
            <if test="dictType != null and dictType != ''">
                AND dictType = #{dictType}
            </if>
            <if test="dictLabel != null and dictLabel != ''">
                AND dictLabel like concat('%', #{dictLabel}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by dictSort asc
    </select>

    <select id="selectDictDataByType" resultType="com.example.system.entity.SysDictData">
        <include refid="selectDictDataVo"/>
        where status = '0' and dictType = #{dictType} order by dictSort asc
    </select>

    <select id="selectDictLabel" resultType="String">
        select dictLabel
        from `system`.sys_dict_data
        where dictType = #{dictType}
          and dictValue = #{dictValue}
    </select>

    <select id="selectDictDataById" parameterType="Long" resultType="com.example.system.entity.SysDictData">
        <include refid="selectDictDataVo"/>
        where dictCode = #{dictCode}
    </select>

    <select id="countDictDataByType" resultType="Integer">
        select count(1)
        from `system`.sys_dict_data
        where dictType = #{dictType}
    </select>

    <delete id="deleteDictDataById" parameterType="Long">
        delete
        from `system`.sys_dict_data
        where dictCode = #{dictCode}
    </delete>

    <delete id="deleteDictDataByIds" parameterType="Long">
        delete from `system`.sys_dict_data where dictCode in
        <foreach collection="array" item="dictCode" open="(" separator="," close=")">
            #{dictCode}
        </foreach>
    </delete>

    <update id="updateDictData">
        update `system`.sys_dict_data
        <set>
            <if test="dictSort != null">dictSort = #{dictSort},</if>
            <if test="dictLabel != null and dictLabel != ''">dictLabel = #{dictLabel},</if>
            <if test="dictValue != null and dictValue != ''">dictValue = #{dictValue},</if>
            <if test="dictType != null and dictType != ''">dictType = #{dictType},</if>
            <if test="cssClass != null">cssClass = #{cssClass},</if>
            <if test="listClass != null">listClass = #{listClass},</if>
            <if test="isDefault != null and isDefault != ''">isDefault = #{isDefault},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">updateBy = #{updateBy},</if>
            updateTime = sysdate()
        </set>
        where dictCode = #{dictCode}
    </update>

    <update id="updateDictDataType" parameterType="String">
        update `system`.sys_dict_data
        set dictType = #{newDictType}
        where dictType = #{oldDictType}
    </update>

</mapper>